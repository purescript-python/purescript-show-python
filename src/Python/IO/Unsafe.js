'use strict';

exports.printLn = function (o) {
    throw Error("Python.IO.Unsafe.printLn is so far a Python backend specific method.")
}

exports.show = function (o) {
    throw Error("Python.IO.Unsafe.show is so far a Python backend specific method.")
}

exports.repr = function (o) {
    throw Error("Python.IO.Unsafe.repr is so far a Python backend specific method.")
}

exports.assertMsg = function (a, b) {
    throw Error("Python.IO.Unsafe.assertMsg is so far a Python backend specific method.")
}

exports.assert_ = function (a) {
    throw Error("Python.IO.Unsafe.assert_ is so far a Python backend specific method.")
}
